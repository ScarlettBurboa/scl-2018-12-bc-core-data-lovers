// esta es una función de ejemplo
// puedes ver como agregamos la función a nuestro objeto global window

/*const example = () => {
  return 'example';
};

window.example = example; */

/*const dataInjuries = INJURIES;
const yearsOfAccidents = dataInjuries.filter(dataInjuries => {
  return dataInjuries;
});
console.log(yearsOfAccidents);*/

const datacategories = INJURIES;
const acuatics = [];
for(let i=0; i<datacategories.length;i++){
  datacategories.map(datacategories => {
    return datacategories;
  });
}
console.log(acuatics);
